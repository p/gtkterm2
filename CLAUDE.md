# Claude Code Guidelines

## Git

- Never amend commits. Always create new commits.
- Commit Makefile and other build-system related changes separately from code changes.

## Code Style

- Keyboard shortcuts (accelerators) should be configurable via the config file, not hardcoded.
